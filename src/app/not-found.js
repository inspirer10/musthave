import PageNotFound from '@/components/PageNotFound/PageNotFound';
import React from 'react';

export const metadata = {
    title: 'Page not found | MUSTHAVE',
};

export default function Custom404() {
    return <PageNotFound />;
}
